// TEST: AuditService resolves through exact name (AuditService)
// Expected: compilation passes

effect AuditService {
    fn run(data: Claim<int>) -> int
}

fn use_exact_name(c: Claim<int>) -> int
uses AuditService:
    { return AuditService.run(c); }
