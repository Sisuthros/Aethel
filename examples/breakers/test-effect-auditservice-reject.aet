// TEST: auditservice (no underscore) does NOT resolve as AuditService
// Expected: AE-NAME-003 (unknown effect)

effect AuditService {
    fn run(data: Claim<int>) -> int
}

fn bad_receiver(c: Claim<int>) -> int
uses AuditService:
    { return auditservice.run(c); }
