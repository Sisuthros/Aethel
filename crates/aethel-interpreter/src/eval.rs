//! Interpreter evaluation logic.

use aethel_ir::lower::*;
use aethel_syntax::span::Span;
use anyhow::Result;

/// Evaluate an IR module (stub for vertical slice).
pub fn eval_module(_module: &IrModule) -> Result<()> {
    Ok(())
}